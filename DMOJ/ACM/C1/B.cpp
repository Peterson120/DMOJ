#include <stdio.h>

int main() {
	int T;
	scanf("%d", &T);
	for (int i = 0; i < T; i++) {
		int N;
		scanf("%d", &N);
		for (int j = 0; j < N; j++) {
			int A, S, O;
			scanf("%d%d%d", &A, &S, &O);
		}

		for (int j = 0; j < 24; j++) {
			if (!awake())
		}
	}
}

bool awake(int A, int S, int O, int N) {
	return ;
}

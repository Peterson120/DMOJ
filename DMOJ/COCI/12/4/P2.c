#include <stdio.h>

int main() {
	// Check that word length is even and number of As and Bs are even
	// Then check that each every four letters has a pair of As or Bs next to each other
	return 0;
}


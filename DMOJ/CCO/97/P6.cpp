#include <stdio.h>
#include <vector>

std::vector<char *> str1, str2, best1, best2;

int main() {
	int size1, size1;
	char *temp1, *temp2;
	std::scanf("%100s%100s", temp1, temp2);
	str1 = temp1;
	for (int i = 0; i < ) {
		
	}
}
